# Server Environment Variables (process.env)
# Place this .env file in the PROJECT ROOT (same level as package.json)

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/digitalteam_db

# PostgreSQL Configuration (alternative to DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=digitalteam_db

# Application Configuration
NODE_ENV=development
PORT=5000

# Client Environment Variables (for frontend)
# These must start with VITE_ to be available in the browser
# Currently none needed - the client makes API calls to the same server

# Note: The client (browser) CANNOT access server environment variables
# Only VITE_ prefixed variables are available in client code via import.meta.env