# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/digitalteam_db

# PostgreSQL Configuration (if using separate variables)
PGHOST=localhost
PGPORT=5432
PGUSER=username
PGPASSWORD=password
PGDATABASE=digitalteam_db

# Application Configuration
NODE_ENV=development
PORT=5000

# For Vercel deployment, add these to your Vercel project settings:
# DATABASE_URL - Your production PostgreSQL connection string (e.g., from Neon, PlanetScale, or Supabase)